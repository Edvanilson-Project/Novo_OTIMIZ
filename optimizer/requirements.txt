# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn[standard]==0.47.0
starlette>=0.41.0,<1.0.0
pydantic==2.10.3
pydantic-settings==2.7.0

# ── Scientific computing (algorithms) ────────────────────────────────────────
numpy>=1.26.0
scipy>=1.13.0
networkx>=3.3           # Grafo de compatibilidade VSP
numba>=0.59.0           # JIT compilation for performance (Optibus level)

# ── ILP / Set Partitioning (CSP) ─────────────────────────────────────────────
pulp>=2.9.0             # Linear Programming solver
highspy>=1.7.0          # HiGHS solver — fallback se CBC indisponível
ortools>=9.10.0         # OR-Tools CP-SAT — solver de scheduling classe Gurobi

# ── HTTP client (chamar o backend NestJS e OSRM/routing) ─────────────────────
httpx>=0.27.0
requests>=2.31.0

# ── Database ─────────────────────────────────────────────────────────────────
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14.0

# ── Async / background tasks ─────────────────────────────────────────────────
anyio>=4.7.0
celery[redis]>=5.4.0          # Fila de tarefas assíncronas (workers VSP/CSP paralelos)

# ── Observability ────────────────────────────────────────────────────────────
structlog>=24.4.0
prometheus-fastapi-instrumentator>=7.0.0
sentry-sdk[fastapi,celery]>=2.0.0  # Error tracking — graceful no-op if SENTRY_DSN not set

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
